## java api 예제
Book book1 = new Book(1, "홍길동전", "허균", 12000);
Book book2 = new Book(2, "목민심서", "정약용", 30000);
Book book3 = new Book(2, "목민심서", "정약용", 30000);

// object.toString() 기본 기능은 클래스명@해시코드를 반환
// 객체의 필드 값을 보여주도록 오버라이딩하여 사용
// Object.equals()의 기본 기능은 '=='와 같이 두 객체의 주소값을 비교(동일성 비교)
// 하지만 때로는 주소가 달라도 필드의 내용이 같으면 같다고 판단해야 할 때가 있음
System.out.println(book1);
System.out.println("동일성 비교" + (book2 == book3));
System.out.println("동등성 비교" + (book2.equals(book3)));

// 자바의 중요한 규약 equals() 비교로 true를 반환한다면
// 두 객체의 hashcode()값은 반드시 같아야 함
// HashSet, HashMap 같은 해시 기반 컬렌션은 객체를 찾을 때 다음 순서로 찾는다
// 1. hashCode()로 어느칸을 찾아볼지 먼저 결정
// 2. 같은 칸 안에서 equals() 로 정말 같은 객체인지 최종 비교
// 만약 equals는 true인데 hashCode가 다르면, 애초에 다른 칸을 보게 되므로 equals() 비교까지 도달하지 못함
System.out.println(book2.hashCode());
System.out.println(book3.hashCode());

Set<Book> books = new HashSet<>();
books.add(book2);

System.out.println(books.contains(book3));

public class Book {

    private int number;
    private String title;
    private String author;
    private int price;

    public Book() {

    }

    public Book(int number, String title, String author, int price) {
        this.number = number;
        this.title = title;
        this.author = author;
        this.price = price;
    }

    public int getNumber() {
        return number;
    }

    public void setNumber(int number) {
        this.number = number;
    }

    public String getTitle() {
        return title;
    }

    public void setTitle(String title) {
        this.title = title;
    }

    public String getAuthor() {
        return author;
    }

    public void setAuthor(String author) {
        this.author = author;
    }

    public int getPrice() {
        return price;
    }

    public void setPrice(int price) {
        this.price = price;
    }

    @Override
    public String toString() {
        return "Book{" +
                "price=" + price +
                ", author='" + author + '\'' +
                ", title='" + title + '\'' +
                ", number=" + number +
                '}';
    }

    @Override
    public boolean equals(Object o) {
        if (o == null || getClass() != o.getClass()) return false;
        Book book = (Book) o;
        return number == book.number && price == book.price && Objects.equals(title, book.title) && Objects.equals(author, book.author);
    }

    @Override
    public int hashCode() {
        return Objects.hash(number, title, author, price);
    }
}

String rawData = "   [PRO-001] Apple iPhone 17 pro | a17 bionic | 250000000   ";

System.out.println(rawData);

// 앞 뒤 공백 제거
String trimmedData = rawData.trim();
System.out.println(trimmedData);

// toUpperCase() 대문자 변환(검색/비교 시 대소문자 통일)
String upperData = trimmedData.toUpperCase();
System.out.println(upperData);

// indexOf() 특정 문자의 인덱스 찾기
int codeStart = upperData.indexOf('[') +1;
int codeEnd = upperData.indexOf(']');
int nameEnd = upperData.indexOf('|');

// 존재하지 않으면 -1 반환
int name = upperData.indexOf("*");
System.out.println(name);

// substring(start, end) 특정 구간 잘라내기
String productCode = upperData.substring(codeStart, codeEnd);
System.out.println(productCode);
String productName = upperData.substring(codeEnd+1, nameEnd).trim();
System.out.println(productName);

// replace() 특정 문자열 치환
String replaced = productName.replace("APPLE IPHONE", "GALAXY S26 ULTRA");
System.out.println(replaced);

String str1 = "java"; // 리터럴 방식
String str2 = "java";
String str3 = new String("java"); // new 연산자 방식
String str4 = new String("java");

System.out.println(str1 == str2);
System.out.println(str3 == str4);
System.out.println(str3.equals(str4));

// 불변성 확인
str2 += " mysql";
System.out.println(str1 == str2);

// split() 문자열 분리하기
String emp = "100/홍길동/서울/영업부";
String[] empArr = emp.split("/");
for (String arr : empArr) {
    System.out.println(arr.isEmpty()? "정보 없음": arr);
}

// 이스케이프 문자
System.out.println("안녕하세요\n저는 개발자입니다");
System.out.println("주특기는 \t'java'와 \"Spring\"입니다.");

// java.time 패키지의 클래스
// LocalDate : 날짜 정보만 표현
// LocalTime : 시간 정보만 표현
// LocalDateTime : 날짜와 시간 모두 표현

LocalDate localDate = LocalDate.of(2026, 5, 8);
LocalTime localTime = LocalTime.of(13, 8);

LocalDateTime localDateTime = LocalDateTime.of(localDate, localTime);
System.out.println(localDateTime);

System.out.println(localDateTime.getYear());
System.out.println(localDateTime.getDayOfWeek());

LocalDateTime arrivalDeadline = localDateTime.minusMinutes(20);
System.out.println(arrivalDeadline);

boolean isPast = localDateTime.isBefore(LocalDateTime.now());

DateTimeFormatter dateTimeFormatter = DateTimeFormatter.ofPattern("yyyy-mm-dd");

String ticket = localDateTime.format(dateTimeFormatter);
System.out.println(ticket);

// 반대로 특정 형식의 문자열을 날짜 객체로 변환
String input = "20260508";
DateTimeFormatter dateTimeFormatter1 = dateTimeFormatter.ofPattern("yymmdd");

// LocalDate.parse()에 문자열과 패턴 전달
LocalDate parsed = LocalDate.parse(input, dateTimeFormatter1);
System.out.println(parsed);

 String heightStr = "170.5";
String weightStr = "68.2";

System.out.println(heightStr+weightStr);

// 파싱 : 문자열 데이터를 실제 해당 타입의 기본 자료형으로 변환
double height = Double.parseDouble(heightStr);
double weight = Double.parseDouble(weightStr);

double result = weight / ((height/100) * (height/100));
System.out.println(result);

// int 20을 result 객체로 포장
//        Integer age = new Integer(20);
Integer age = 10;

int unboxedAge = age; // age.intValue() 오토 언박싱

// 주의사항 래퍼 클래스는 '객체'이기 때문에, 값 비교를 할 때 == 연산자를 쓰면 위험
Integer num3 = 128;
Integer num4 = 128;
System.out.println(num3 == num4);
// 래퍼 클래스 객체의 값이 같은지 비교할 때는 반드시 equals 사용
System.out.println(num3.equals(num4));

// 숫자를 다시 문자열로
// 계산이 끝난 데이터를 화면에 표시하거나, 저장하려면 다시 문자열로 반환해야 함
// 1. String.valueOf(값)
// 2. 값 + "" 간편한 방법
String bmiStr = String.valueOf(result);
String bmiStr2 = result +"";

System.out.println("당신의 bmi는 " + bmiStr + "입니다.");